# Runbook — Bateria de Maturidade

Comandos (smoke, setup único, 2 GPUs `--skip-setup`) e pegadinhas resolvidas
(disco/Ray, paralelismo, overrides, profiles) vivem na **§9 da proposta**:
`docs/markdowns/proposta_artigo_maturidade.md` → "9. Execução (operacional)".

Mantido lá como fonte única para não divergir; este arquivo é só o ponteiro
ao lado do runner (`run_bateria.sh`).
